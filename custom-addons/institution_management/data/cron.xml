<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="ir_cron_auto_complete_batch" model="ir.cron">
            <field name="name">Institution: Auto-Complete Expired Batches</field>
            <field name="model_id" ref="model_institution_batch"/>
            <field name="state">code</field>
            <field name="code">model._check_batch_expiry()</field>
            <field name="user_id" ref="base.user_root"/>

            <field name="interval_number">1</field>
            <field name="interval_type">days</field>

            <field name="numbercall">-1</field>

            <field name="doall" eval="False"/>
        </record>

    </data>
</odoo>