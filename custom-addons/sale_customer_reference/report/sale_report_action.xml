<odoo>
    <record id="action_report_simple_sale" model="ir.actions.report">
        <field name="name">My Simple Sale Report</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">sale_customer_reference.report_simple_sale</field>
        <field name="report_file">sale_customer_reference.report_simple_sale</field>

        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">report</field>

        <field name="print_report_name">'Sale Order - %s' % (object.name)</field>
    </record>
</odoo>
